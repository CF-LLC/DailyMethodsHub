[build]
  command = "npm run build"
  publish = ".next"

[functions]
  node_bundler = "esbuild"
  
[build.environment]
  NODE_VERSION = "20"
  SECRETS_SCAN_OMIT_KEYS = "SUPABASE_SERVICE_ROLE_KEY,CRON_SECRET"
